// still not  finished

const apiURL = 'this is where the api will go when i get the key'
fetch(apiURL)
    .then(response => {
        if (!response.ok) {
            throw new Error('No Response');
        };
    
    return response.php();
})
.then(data => {
    console.log(data);
})
.catch(error => {
    console.error('Error', error);
});

// test comment